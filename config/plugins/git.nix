{
  plugins = {
    gitsigns = {
      # enable = true;
      # settings.trouble = true;
      settings = {
        signs = {
          delete = {
            show_count = true;
          };
        };
      };
    };
  };
}
